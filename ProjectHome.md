This php class creates a typo for a keyword based on most common typing errors.

When users want to access certain sites, sometimes they make spelling mistakes while typing the domain names.

This is a clever class that can generate variants of a domain name by changing some letters with others that are physically near in a keyboard.